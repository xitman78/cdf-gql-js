# Demo GraphQL Wrapper around CDF API

Install packages:
```
yarn
```

In the project root filder create `.env` file with CDF credentials:
```
CDF_PROJECT=cdf_project
CDF_API_KEY=cdf_api_key
```

Start the app:
```
yarn start
```
